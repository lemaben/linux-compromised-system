# Step 3 — Filesystem Hunting

Explore noisy directories and find the malicious file hidden among normal junk.

## Objectives
- Check `/home`, `/tmp`, `/var/tmp`
- Distinguish normal clutter from the real indicator
- Submit Flag 2 as `flag{filename}`

## Minimal hint
Use the most direct Linux tools you already know before searching wider.
