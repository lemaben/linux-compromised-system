# Step 4 — Persistence

The attacker returns after reboot. Find the persistence mechanism.

## Objectives
- Check hidden files in the main user's home directory
- Look for shell startup abuse
- Submit Flag 3 as `flag{file}`

## Minimal hint
Use the most direct Linux tools you already know before searching wider.
