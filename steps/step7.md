# Step 7 - Remediation

Remove access, remove artifacts, and leave the host in a safer state.

## Objectives
- Remove the suspicious user
- Remove the malicious script
- Remove or neutralize persistence
- Fix weak permissions
- Stop the suspicious process

## Deliverable
When the system is secured, record:

`flag{system_secured}`

## Hint
A cleanup that leaves persistence behind is not cleanup.
